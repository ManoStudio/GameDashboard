{
  "name": "game-dashboard-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply game-dashboard-db --local && wrangler d1 migrations apply game-dashboard-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
